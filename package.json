{
  "name": "transportation-dashboard",
  "version": "2.0.0",
  "description": "Interactive transportation finance dashboard with market data",
  "main": "index.html",
  "scripts": {
    "build": "echo 'No build step required for static site with serverless functions'",
    "dev": "netlify dev",
    "test": "echo 'Tests not configured'"
  },
  "dependencies": {
    "cheerio": "^1.0.0-rc.12"
  },
  "devDependencies": {
    "@netlify/functions": "^2.8.0"
  },
  "engines": {
    "node": "18.x"
  }
}
